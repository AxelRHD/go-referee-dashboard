root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/referee-dashboard ./cmd"
  bin = "tmp/referee-dashboard"
  full_bin = "tmp/referee-dashboard serve"
  include_ext = ["go", "sql", "css"]
  exclude_dir = ["tmp", "bin", "model"]
  delay = 1000

[misc]
  clean_on_exit = true
