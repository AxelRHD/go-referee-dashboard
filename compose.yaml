services:
  referee-dashboard:
    build: .
    image: referee-dashboard:latest
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./data:/data
    environment:
      - DB_PATH=/data/referee.db
